# ------------------------------------------------------------------------------
# Verilog Deployment Configuration
# ------------------------------------------------------------------------------

# IMAGE_REGISTRY
# Defines the container registry used to pull Docker images.
#
# Available options:
#   docker.io   → Docker Hub (default)
#   ghcr.io     → GitHub Container Registry
#
# Both registries contain the same, up-to-date images.
# If this variable is not set, Docker Hub (docker.io) will be used automatically.
# ------------------------------------------------------------------------------


IMAGE_REGISTRY=docker.io
